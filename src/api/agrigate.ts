/* eslint-disable linebreak-style */
// import { storage } from "functional/storage";

// function getUrlForAggregatedWord(criteria) {
// const userId = storage.user?.userId;
// return `${API}${URL_USER}${URL_NEXT}${userId}${URL_NEXT}${URL_AGGREGATED}${URL_PARAM}${URL_FILTER}${criteria}${MAX_WORDS_PARAMETER}`;
// };
